export interface Booking {
    id: number;
    confirmationNumber: number;
    flightId: string;
    passengerId: string;
}

export interface BookingDTO {
    id: number;
    confirmationNumber: number;
    flightId: string;
    passengerId: string;
}
